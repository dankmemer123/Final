<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vishnunair.ftutoria">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Loggin"
            android:label="@string/title_activity_loggin"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Messaging"
            android:label="@string/title_activity_messaging"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TutorFinderAdder"
            android:label="@string/title_activity_tutor_finder_adder"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".listtutorscreen"
            android:label="@string/title_activity_listtutorscreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".listoftutors"
            android:label="@string/title_activity_listoftutors"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUp"
            android:label = "Sign Up"
            android:theme="@style/AppTheme.NoActionBar"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>